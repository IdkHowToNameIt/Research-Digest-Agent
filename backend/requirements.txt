# Dipendenze del Research Digest Agent — DRA.
# La pipeline è ~80% script; il modello LLM serve solo alla sintesi finale.
# openai è il client OpenAI-compatibile usato per OpenRouter (modalità reale;
# i test mockano il generatore e non lo richiedono).
pydantic>=2.7.0
feedparser>=6.0.0
PyYAML>=6.0
python-dotenv>=1.0
openai>=1.40
pytest>=8.0
