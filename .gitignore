__pycache__/
*.pyc
.venv/
venv/
.env
backend/data/*.sqlite3
backend/data/archivio/
backend/data/metriche/
out/
sito/
.idea/
.claude/settings.local.json
frontend/_artifact_*.html
claude-progress.txt

# Frontend React: dipendenze e build. package-lock.json INVECE va committato,
# perche' la CI usa `npm ci`, che installa esattamente quel lock.
frontend/node_modules/
frontend/dist/

# PDF di esempio usati per analisi manuale, non materiale di progetto
ex pdf/

# video di riferimento: 65 MB, i binari non si comprimono in delta
video/

# manuale utente: resta locale, non si pubblica su GitHub
manuale-utente/
