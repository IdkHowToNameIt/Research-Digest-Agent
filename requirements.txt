# Dipendenze del Research Digest Agent — DRA.
# La pipeline è ~80% script; il modello (Gemini) serve solo alla sintesi finale.
# google-genai è richiesto solo per la modalità reale (non per i test, che mockano).
pydantic>=2.7.0
feedparser>=6.0.0
PyYAML>=6.0
python-dotenv>=1.0
google-genai>=0.3.0
pytest>=8.0
